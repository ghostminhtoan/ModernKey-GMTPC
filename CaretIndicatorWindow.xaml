<Window x:Class="ModernKey.CaretIndicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CARET INDICATOR"
        Width="50" Height="26"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" IsHitTestVisible="False"
        ShowActivated="False" Focusable="False"
        WindowStartupLocation="Manual">
    <Border x:Name="IndicatorBorder" Background="#E6080210" BorderBrush="{StaticResource CyberNeonCyan}" BorderThickness="1.5" CornerRadius="3">
        <TextBlock x:Name="TxtLang" Text="[VI]" FontWeight="Bold" FontSize="11" FontFamily="Consolas"
                   Foreground="{StaticResource CyberNeonCyan}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Effect>
                <DropShadowEffect x:Name="GlowEffect" Color="#00F0FF" BlurRadius="6" ShadowDepth="0" Opacity="0.8"/>
            </TextBlock.Effect>
        </TextBlock>
    </Border>
</Window>
